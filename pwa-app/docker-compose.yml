services:
  ha-wall-kiosk-pwa:
    build: .
    container_name: ha-wall-kiosk-pwa
    restart: unless-stopped
    ports:
      - "8090:80"
    environment:
      BASIC_AUTH_USER: ${BASIC_AUTH_USER}
      BASIC_AUTH_PASS: ${BASIC_AUTH_PASS}
